server:
  port: 8082

spring:
  application:
    name: movie-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ticketpigeon_movie
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

gateway:
  secret: ELRpz7MMNGUwuQ4jXdEX4JojHTeQKGRICEceYEpupUT

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
